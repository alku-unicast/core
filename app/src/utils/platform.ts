export const isMacOS   = (): boolean => /macintosh/i.test(navigator.userAgent);
export const isLinux   = (): boolean => /linux/i.test(navigator.userAgent);
export const isWindows = (): boolean => /windows/i.test(navigator.userAgent);
